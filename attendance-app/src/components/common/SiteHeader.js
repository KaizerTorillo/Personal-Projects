
import "./SiteHeader.css"

function SiteHeader() {
    return (
        <header className="header">
            <div className="title">AWS re/Start Cohort</div>
        </header>
    );
}

export default SiteHeader;